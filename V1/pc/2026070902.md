---
outline: deep
---

# 集成vee-validate

本章主要集成vee-validate@4，让登录、注册、找回密码都有表单验证功能

## 安装vee-validate

安装vee-validate、@vee-validate/rules和@vee-validate/i18n

```shell
npm i vee-validate@4 @vee-validate/rules@4 @vee-validate/i18n@4
```

## 基本使用

1. 把form原生标签的表单替换成Form

2. 把input原生标签的表单替换成Field

3. 注意是要从vee-validate引入这两个标签：

   ```ts
   import { Field, Form } from 'vee-validate'
   ```

4. 修改Form标签：

   ```vue
   <Form ref="object" v-slot="{ validate, errors }"></Form>
   ```

5. 修改Field的一些属性：

   加入as="input"；v-model双向绑定数据；placeholder输入提醒； rules="required|email"表单规则；

   ```vue
   <div class="layui-input-inline">
       <Field
              type="text"
              as="input"
              rules="required|email"
              name="username"
              placeholder="请输入用户名"
              class="layui-input"
              v-model="username"
              ></Field>
   </div>
   <!-- 错误信息展示 -->
   <div class="layui-form-mid">
       <span style="color: #c00">{{ errors.username }}</span>
   </div>
   ```

6. 在src下创建common目录，在common目录下创建vee-validate.ts，这个文件就是用来存放规则的

   ```ts
   // src\common\vee-validate.ts
   import { defineRule } from 'vee-validate'
   import { required, email, min, length, confirmed, max, is_not } from '@vee-validate/rules'
   
   defineRule('required', required)
   defineRule('email', email)
   defineRule('min', min)
   defineRule('length', length)
   defineRule('confirmed', confirmed)
   defineRule('max', max)
   defineRule('is_not', is_not)
   ```

7. 在main.ts引入

   ```ts
   import '@/common/vee-validate.ts'
   ```

8. 测试login页面的邮箱输入框，输入是否有校验，有的话则成功使用，没有的话则需要观察是否遗漏

## 优化表单校验

login.vue：仅展示修改代码

```vue
<template>
<Form ref="object" v-slot="{ validate, errors }">
    <div class="layui-input-inline">
        <Field
               type="text"
               as="input"
               rules="required|email"
               name="username"
               placeholder="请输入用户名"
               class="layui-input"
               v-model="username"
               ></Field>
    </div>
    <!-- 错误信息展示 -->
    <div class="layui-form-mid">
        <span style="color: #c00">{{ errors.username }}</span>
    </div>
    </div>

<div class="layui-input-inline">
    <Field
           type="password"
           as="input"
           rules="required|min:6"
           name="password"
           class="layui-input"
           v-model="password"
           placeholder="请输入密码"
           />
    </div>
<!-- 错误信息展示 -->
<div class="layui-form-mid">
    <span style="color: #c00">{{ errors.password }}</span>
    </div>

<div class="layui-input-inline">
    <Field
           type="text"
           as="input"
           rules="required|length:4"
           name="code"
           placeholder="请输入验证码"
           class="layui-input"
           v-model="code"
           />
    </div>
<div>
    <span class="svg" style="color: #c00" @click="_getCode" v-html="svg"></span>
    </div>
<div class="layui-form-mid">
    <span style="color: #c00">{{ errors.code }}</span>
    </div>
    </div>
</template>
<script lang="ts" setup>
import { Field, Form } from 'vee-validate'
import { reactive, toRefs } from 'vue'
const state = reactive({
  username: '',
  password: '',
  code: '',
  svg: '',
})
const { username, password, code, svg } = toRefs(state)

const login = async () => {}
const _getCode = async () => {}
</script>
```

reg.vue：仅展示修改代码

```vue
<template>
<div class="layui-input-inline">
    <Field
           type="text"
           as="input"
           name="username"
           placeholder="请输入邮箱"
           rules="required|email"
           autocomplete="off"
           class="layui-input"
           />
    </div>
<div class="layui-form-mid layui-word-aux">将会成为您唯一的登入名</div>
<!-- 错误信息展示 -->
<div class="layui-form-mid">
    <span style="color: #c00">{{ errors.username }}</span>
    </div>

<div class="layui-input-inline">
    <Field
           type="text"
           as="input"
           rules="required|min:3|name"
           placeholder="请输入昵称"
           name="name"
           autocomplete="off"
           class="layui-input"
           v-model="username"
           />
    </div>
<div class="layui-form-mid">
    <span style="color: #c00">{{ errors.name }}</span>
    </div>

<div class="layui-input-inline">
    <Field
           type="password"
           as="input"
           name="password"
           rules="required|min:6|max:16"
           placeholder="请输入密码"
           autocomplete="off"
           class="layui-input"
           v-model="password"
           />
    </div>
<div class="layui-form-mid layui-word-aux">6到16个字符</div>
<!-- 错误信息展示 -->
<div class="layui-form-mid">
    <span style="color: #c00">{{ errors.password }}</span>
    </div>

<div class="layui-input-inline">
    <Field
           type="password"
           as="input"
           name="repassword"
           rules="required|confirmed:@password"
           placeholder="请重新输入密码"
           autocomplete="off"
           class="layui-input"
           v-model="repassword"
           />
    </div>
<!-- 错误信息展示 -->
<div class="layui-form-mid">
    <span style="color: #c00">{{ errors.repassword }}</span>
    </div>

<div class="layui-input-inline">
    <Field
           type="text"
           name="code"
           as="input"
           placeholder="请输入验证码"
           rules="required|length:4"
           autocomplete="off"
           class="layui-input"
           v-model="code"
           />
    </div>
<div class="layui-form-mid">
    <span class="svg" style="color: #c00" @click="_getCode" v-html="svg"></span>
    </div>
<!-- 错误信息展示 -->
<div class="layui-form-mid">
    <span style="color: #c00">{{ errors.code }}</span>
    </div>
</template>
<script lang="ts" setup>
import { Field, Form } from 'vee-validate'
import { reactive, toRefs } from 'vue'
const state = reactive({
  username: '',
  password: '',
  repassword: '',
  code: '',
  svg: '',
})
const { username, password, repassword, code, svg } = toRefs(state)

const _getCode = async () => {}
</script>
```

forget.vue：仅展示修改代码

```vue
<template>
div class="layui-input-inline">
<Field
       type="text"
       as="input"
       name="username"
       rules="required|email"
       placeholder="请输入用户名(邮箱)"
       autocomplete="off"
       class="layui-input"
       v-model="username"
       />
    </div>
<!-- 错误信息展示 -->
<div class="layui-form-mid">
    <span style="color: #c00">{{ errors.username }}</span>
    </div>
<div class="layui-input-inline">
    <Field
           type="text"
           as="input"
           name="code"
           placeholder="请输入验证码"
           rules="required|length:4"
           autocomplete="off"
           class="layui-input"
           v-model="code"
           />
    </div>
<div class="layui-form-mid">
    <span class="svg" style="color: #c00" @click="_getCode" v-html="svg"></span>
    </div>
<div class="layui-form-mid">
    <span style="color: #c00">{{ errors.code }}</span>
    </div>
</template>
<script lang="ts" setup>
    import { Field, Form } from 'vee-validate'
    import { reactive, toRefs } from 'vue'
    const state = reactive({
        username: '',
        code: '',
        svg: '',
    })
    const { username, code, svg } = toRefs(state)
    const _getCode = async () => {}
</script>
```

## i18n国际化

如果按照上面的方式实现，会发现两个问题，第一个问题是：错误提示是英文的，第二个问题是：name和confirmed:@password不能正常使用，接下来解决这两个问题：

src\common\vee-validate.ts

```ts
import { defineRule, configure } from 'vee-validate'
import { required, email, min, length, confirmed, max, is_not } from '@vee-validate/rules'
import { localize } from '@vee-validate/i18n'
import zh from '@vee-validate/i18n/dist/locale/zh_CN.json'

defineRule('required', required)
defineRule('email', email)
defineRule('min', min)
defineRule('length', length)
defineRule('confirmed', confirmed)
defineRule('max', max)
defineRule('is_not', is_not)
//自定义规则
defineRule('name', (value: unknown) => {
  if (!value) {
    return true
  }
  return !/^\d+$/.test(String(value)) || '不能以纯数字为昵称'
})
//i18n
const config = {
  messages: {
    ...zh.messages,
    required: '请输入{field}',
    min: '请在{field}输入至少有0:{length}个字符',
  },
  names: {
    email: '邮箱',
    username: '用户名',
    code: '验证码',
    vcode: '邮箱验证码',
    name: '昵称',
    password: '密码',
    repassword: '密码',
    // usernamecode: '邮箱验证码',
    oldpassword: '旧密码',
    title: '标题',
    catalog: '分类',
  },
  fields: {
    catalog: {
      is_not: '请选择{field}',
    },
    repassword: {
      confirmed: '两次输入的{field}不一致！',
    },
    code: {
      length: () => '请输入4位验证码',
    },
  },
}
configure({
  // Generates an English message locale generator
  generateMessage: localize('zh_CN', config),
})
```

