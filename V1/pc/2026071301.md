---
outline: deep
---

# 封装Axios

本章节主要对前端的axios拦截器和业务api接口进行封装

## FixBug

在开始之前，需要修复一下之前漏掉的一个数据

打开Reg.vue文件

```vue
<div class="layui-input-inline">
    <Field
           type="text"
           as="input"
           name="username"
           placeholder="请输入邮箱"
           rules="required|email"
           autocomplete="off"
           class="layui-input"
           v-model="username"
           />
</div>

<div class="layui-input-inline">
    <Field
           type="text"
           as="input"
           rules="required|min:3|name"
           placeholder="请输入昵称"
           name="name"
           autocomplete="off"
           class="layui-input"
           v-model="name"
           />
</div>
```

这里的昵称是name，邮箱是username，并添加name这个响应式数据

## 封装拦截器

官方示例：https://axios.rest/zh/pages/advanced/interceptors.html

创建src\common\axios.ts

```ts
import axios, { AxiosError } from 'axios'
import errorHandle from './errorHandle'
const instance = axios.create({
  baseURL:
    import.meta.env.MODE === 'development' ? 'http://localhost:3000' : 'https://you.domain.com',
})
// 添加请求拦截器
instance.interceptors.request.use(
  (config) => {
    // 在请求发送之前执行某些操作
    console.log('config', config)

    return config
  },
  (error: AxiosError) => {
    // 处理请求错误
    errorHandle(error)
    return Promise.reject(error)
  },
)

// 添加响应拦截器
instance.interceptors.response.use(
  (res) => {
    // 状态码在 2xx 范围内的响应会触发此函数
    // 处理响应数据
    console.log('res', res)
    if (res.status === 200) {
      return Promise.resolve(res.data)
    } else {
      return Promise.reject(res)
    }
  },
  (error: AxiosError) => {
    // 状态码不在 2xx 范围内的响应会触发此函数
    // 处理响应错误
    errorHandle(error)
    return Promise.reject(error)
  },
)
export default instance
```

- 这里就是简单复制官网的拦截器示例
- `const instance = axios.create()`创建axios实例
- 把普通函数改为箭头函数
- 用errorHandle处理统一处理axios的error

创建src\common\errorHandle.ts

```ts
import type { AxiosError } from 'axios'

const errorHandle = (error: AxiosError) => {
  console.log(error)
}
export default errorHandle
```

- 只是打印错误，没有过多处理

修改接口请求：src\api\login.ts

```ts
//验证码
const getCode = async () => {
  return await axios.get('/public/getCaptcha')
}
```

使用：src\pages\login.vue

```ts
const _getCode = async () => {
  const result = await getCode()
  const { data } = result
  state.svg = data
  console.log(data)
}
```

- 这里之前是then写法，改为async和await写法

- 把接收的result解构出data，然后赋值

- 这里的data，其实是刚好axios里面有定义data类型，点击data就能跳转看到，因为我们这里还没有封装get，用的还是axios默认配置，其实这里也可以解构出code，只不过ts会报错，但是还是可以拿到，不信可以测试一下，打印出来的code是200，这个ts报错在封装get解决

  另外，之前后端返回的是msg，这里后端需要改为data，也就是返回以下格式

  ```json
  {
    "code": 200,
    "data": "<svg "/></svg>"
  }
  ```

  测试是否可以拿到code

  ```ts
  const _getCode = async () => {
    const result = await getCode()
    const { data, code } = result
    state.svg = data
    console.log(code)
  }
  ```

- 在封装的axios中可以看到baseurl，之前的是写在main.ts上，因为是两个实例，所以main.ts的axios baseURL需要删除。

## 封装Get和Post请求

先优化一下连接配置，创建：src\config\index.ts

```ts
export default {
  basurl: {
    dev: 'http://localhost:3000',
    prop: 'https://you.domain.com',
  },
}

```

修改：src\common\axios.ts

```ts
import config from '@/config'
const instance = axios.create({
  baseURL: import.meta.env.MODE === 'development' ? config.basurl.dev : config.basurl.prop,
})
```

配置headers和timeout：

```ts
const instance = axios.create({
  baseURL: import.meta.env.MODE === 'development' ? config.basurl.dev : config.basurl.prop,
  headers: {
    'Content-Type': 'application/json;charset=utf-8',
  },
  timeout: 10000, // 10秒断定失败
})
```

整体优化：

```ts
import axios, {
  AxiosError,
  type AxiosInstance,
  type AxiosRequestConfig,
  type AxiosResponse,
} from 'axios'
import errorHandle from './errorHandle'

class HttpRequest {
  private baseUrl: string
  constructor(baseUrl: string) {
    this.baseUrl = baseUrl
  }
  // 获取配置
  getConfig() {
    const config = {
      baseURL: this.baseUrl,
      headers: {
        'content-Type': 'application/json;charset=utf-8',
      },
      timeout: 10000, // 10秒断定失败
    }
    return config
  }
  //设置拦截器
  interceptors(instance: AxiosInstance) {
    // 添加请求拦截器
    instance.interceptors.request.use(
      (config) => {
        // 在请求发送之前执行某些操作
        console.log('config', config)
        return config
      },
      (error: AxiosError) => {
        // 处理请求错误
        errorHandle(error)
        return Promise.reject(error)
      },
    )
    // 添加响应拦截器
    instance.interceptors.response.use(
      (res) => {
        // 状态码在 2xx 范围内的响应会触发此函数
        // 处理响应数据
        console.log('res', res)
        if (res.status === 200) {
          return Promise.resolve(res.data)
        } else {
          return Promise.reject(res)
        }
      },
      (error: AxiosError) => {
        // 状态码不在 2xx 范围内的响应会触发此函数
        // 处理响应错误
        errorHandle(error)
        return Promise.reject(error)
      },
    )
  }
  //创建实例
  request(options: AxiosRequestConfig) {
    const instance = axios.create()
    const newOPtions = Object.assign(this.getConfig(), options)
    this.interceptors(instance)
    return instance(newOPtions)
  }
  //封装get,config不一定存在
  get(url: string, config?: AxiosRequestConfig) {
    const options = Object.assign(
      {
        method: 'get',
        url: url,
      },
      config,
    )
    return this.request(options)
  }
  //封装post
  //Promise<AxiosResponse> 是这个方法的返回值类型声明，告诉 TypeScript：这个 post 方法返回的是一个 Promise，Promise 成功后拿到的值是 Axios 的响应对象 AxiosResponse。
  // data?: AxiosRequestConfig['data']Axios 已经提供了 AxiosRequestConfig['data'] 类型any | string | object | ArrayBuffer | FormData | Blob | ...
  post(url: string, data?: AxiosRequestConfig['data']): Promise<AxiosResponse> {
    return this.request({
      method: 'post',
      url: url,
      data: data,
    })
  }
}
export default HttpRequest

```

src\common\request.ts

```ts
import HttpRequest from './axios'
import config from '../config/index'
// 定义baseUrl
const baseUrl = import.meta.env.MODE === 'development' ? config.basurl.dev : config.basurl.prop
// 实例化HttpRequest
const axios = new HttpRequest(baseUrl)
export default axios
```

修改接口的导入：src\api\login.ts

```ts
import axios from '@/common/request'

//验证码
const getCode = async () => {
  return await axios.get('/public/getCaptcha')
}
```

- 这个封装很纯粹，没有对拦截器做任何的修改，后续业务需要再添加

- 封装过程：获取axios的配置->设置拦截器->创建实例->封装get->封装post

- request实例化封装的axios对象

- 需要注意的是：`Promise<AxiosResponse>`这个是这个方法的返回值类型声明，但是后端返回的不一定是这些类型

  ```ts
  post(url: string, data?: AxiosRequestConfig['data']): Promise<AxiosResponse> {
      return this.request({
        method: 'post',
        url: url,
        data: data,
      })
    }
  ```

## 优化Get和Post参数问题

之前封装好的axios其实还存在一个隐藏问题，就是后端返回数据的类型，之前是`Promise<AxiosResponse>`，如果后端返回一个data，刚好对上，那解构赋值没问题，如果后端返回其他的如code，msg等，就会报一个ts错误，那怎么解决呢？

修改src\common\axios.ts

```ts
  //封装get
  get(url: string, config?: AxiosRequestConfig): Promise<AxiosResponse> | Promise<HttpResponse> {
    const options = Object.assign(
      {
        method: 'get',
        url: url,
      },
      config,
    )
    return this.request(options)
  }
  //封装post
  post(
    url: string,
    data?: AxiosRequestConfig['data'],
  ): Promise<AxiosResponse> | Promise<HttpResponse> {
    return this.request({
      method: 'post',
      url: url,
      data: data,
    })
  }
```

新增：src\common\interface.ts

```ts
export interface ForgetInfo {
  username: string
  code: string
}
//后端返回的数据格式
export interface HttpResponse {
  code: number
  //unknown：需要类型判断后才能使用
  data?: unknown
  msg?: string
}

```

改写一下src\api\login.ts

```ts
import type { ForgetInfo } from '@/common/interface'
import axios from '@/common/request'

//验证码
const getCode = async () => {
  return await axios.get('/public/getCaptcha')
}
//找回密码
const forget = async (option: ForgetInfo) => {
  return await axios.post('/login/forget', { ...option })
}
export { getCode, forget }

```

使用：src\pages\forget.vue

```ts
import { getCode, forget } from '@/api/login'
import type { HttpResponse } from '@/common/interface'
//找回密码
const submit = async (validate: any) => {
  const { valid } = await validate()
  if (!valid) {
    console.log('校验失败')
    return
  }
  const result = await forget({ username: state.username, code: state.code })
  console.log(result)
  //明确告知result就是HttpResponse类型
  const { code, msg } = result as HttpResponse
  console.log(code)

  if (code === 200) {
    alert(msg)
  }
```

至此，本章封装axios内容结束，接下来是完成验证码校验和存储
